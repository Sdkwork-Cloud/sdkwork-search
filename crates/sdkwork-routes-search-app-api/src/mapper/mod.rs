//! Response mapping helpers for search app-api (`WEB_BACKEND_SPEC.md`).
//!
//! Reserved for response-shape mapping between provider/service outputs and the HTTP response
//! contract. Currently empty; mapping functions will be added as handlers delegate to services.
